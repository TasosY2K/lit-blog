<!DOCTYPE html>
<html>
<head>
    title= settings.siteName + " | " + postData.title
    link(rel="shortcut icon", href=settings.siteIcon)
    meta(name="description", content=postData.contentText.trim().substr(0, 70))
    meta(property="og:image", content="/img/" + postData.image)
    include header.pug
</head>
<body id="body" class="d-flex flex-column min-vh-100">
    span(id="bgImg")= settings.siteImage
    span(id="bgContent")= postData.content
    include topnav.pug
    <div class="container">
        h2(class="mt-4 text-light")= postData.title
        p(class="dateText text-light")= postData.createdAt
        img(class="img-fluid" src="/img/" + postData.image)
        <hr class="line">
        <div class="mt-1" id="postContainer"></div>
        p(class="dateText text-light")= postData.author
    </div>
    include footer.pug
</body>
</html>