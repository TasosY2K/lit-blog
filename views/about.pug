<!DOCTYPE html>
<html>
<head>
    title= settings.siteName + " | About"
    link(rel="shortcut icon", href=settings.siteIcon)
    meta(name="description", content=postData.contentText.substr(0, 70))
    meta(property="og:image", content=postData.image1)
    include header.pug
</head>
<body id="body" class="d-flex flex-column min-vh-100">
    span(id="bgImg")= settings.siteImage
    span(id="bgContent")= postData.content
    include topnav.pug
    <div class="container">
        h2(class="mt-4 text-light") About
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    img(class="d-block w-100" src=postData.image1)
                </div>
                <div class="carousel-item">
                    img(class="d-block w-100" src=postData.image2)
                </div>
                <div class="carousel-item">
                    img(class="d-block w-100" src=postData.image3)
                </div>
            </div>
            <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
        <hr class="line">
        <div class="mt-1" id="postContainer"></div>
        p(class="dateText text-light")= postData.author
    </div>
    include footer.pug
</body>
</html>